# =============================================================================
# can_driver_node 机械臂专用配置
# =============================================================================

can_driver_node:
  control_frequency: 100.0
  debug_bypass_ros_control: false
  motor_query_hz: 20.0
  pp_fast_write_enabled: true
  pp_position_default_velocity_rad_s: 1.0472
  pp_csp_default_velocity_rad_s: 3
  startup_position_sync_timeout_sec: 1.0
  safety_stop_on_fault: true
  safety_require_enabled_for_motion: true
  max_position_step_rad: 0.0
  safety_hold_after_device_recover: true
  init_loopback: false
  pp_local_zero_offset_persistence_enabled: true
  pp_zero_offset_persist_on_apply_to_motor: false

  joints:
    - name: elbow_pitch_joint
      safety_group: arm
      motor_id: 0x13
      protocol: PP
      can_device: canable1
      control_mode: csp
      position_scale: 65536
      velocity_scale: 65536
      direction_sign: -1.0
      ip_max_velocity: 1.0
      ip_max_acceleration: 0.8
      ip_max_jerk: 4.0
      ip_goal_tolerance: 0.001
      command_alignment_tolerance: 0.01

    - name: wrist_pitch_joint
      safety_group: arm
      motor_id: 0x14
      protocol: PP
      can_device: canable1
      control_mode: csp
      position_scale: 65536
      velocity_scale: 65536
      ip_max_velocity: 1.0
      ip_max_acceleration: 2.0
      ip_max_jerk: 10.0
      ip_goal_tolerance: 0.001
      command_alignment_tolerance: 0.01

    - name: wrist_roll_joint
      safety_group: arm
      motor_id: 0x16
      protocol: PP
      can_device: canable1
      control_mode: csp
      position_scale: 65536
      velocity_scale: 65536
      command_alignment_tolerance: 0.01

    - name: wrist_yaw_joint
      safety_group: arm
      motor_id: 0x15
      protocol: PP
      can_device: canable1
      control_mode: csp
      position_scale: 65536
      velocity_scale: 65536
      direction_sign: -1.0
      command_alignment_tolerance: 0.01

    - name: left_gripper_finger_joint
      safety_group: arm
      motor_id: 0x17
      protocol: PP
      can_device: canable1
      control_mode: csp
      # 夹爪是 prismatic 关节，上层单位是 m；这里不能填 65536
      # （65536 会被解析为 2π/65536 rad/count）。
      # 与 can_driver.yaml 保持一致：raw * scale = finger travel in m。
      position_scale: 0.0000017807297172304439
      velocity_scale: 0.0000017807297172304439
      pp_position_default_velocity: 0.05
      pp_csp_default_velocity: 0.05
      command_alignment_tolerance: 0.01
